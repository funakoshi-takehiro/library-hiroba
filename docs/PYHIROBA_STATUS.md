# 現状まとめ（library-hiroba 0.6.1 公開時点・2026-08-20）

**本体側の対応は不要です。** 契約（`kind` の受け渡し）は 0.6.0 から一切変えていません。
0.6.1 は、こちらの利用者（先生・生徒）の書き間違いを止めるための修正だけです。

## 1. 版

| 版 | 内容 | 状態 |
| --- | --- | --- |
| 0.6.0 | `ai.embed()` / `ai.search()` | 公開済み・そちらで同梱済み |
| 0.6.1 | 公開前監査で見つかった6件の修正 | **PyPI 公開済み** |

`pip install -U library-hiroba` で 0.6.1 が入ります。

## 2. 機能の対応表

`pyhirobaFeatures` の目印ごとに、いまどこまで揃っているかです。

| 目印 | 使う場所 | 本体 | library-hiroba |
| --- | --- | --- | --- |
| `ai-load` / `ai-ask`（必須） | `ai.load()` / `ai.ask()` | ✅ | ✅ |
| `ai-probe` | `ai.recommend()` / `ai.load("auto")` | ✅ | ✅ |
| `forms` | `ui.form()` / `talk.form()` | ✅ | ✅ |
| `ai-embed` | `ai.embed()` / `ai.search()` | ✅ | ✅ |
| `ai-stream` | `ai.stream()` の逐次表示 | ⬜ 未実装 | ✅ 受け入れ側は用意済み |

`ai-stream` だけが残っています。**急ぎではありません。** 未対応のあいだは自動的に
`ai-ask` へ落ちて全文が一度に返るので、利用者のコードは書き換え不要です。入れられる際に
`pyhirobaFeatures` へ `ai-stream` を足していただければ、こちらは何もせずに切り替わります。

## 3. 0.6.1 で直したこと

タグを打つ前に `embed` / `search` の一式を監査しました。探したのは「例外にならず、
それらしい答えが返ってしまう」書き方です。教材ではこれがいちばん困ります —
先生も生徒も、間違っていることに気付けません。**6件見つかりました。**

| # | 書き方 | 前（黙って間違う） | 後 |
| --- | --- | --- | --- |
| 1 | `ai.search("怖い本", "あいうえお")` | 5文字を5冊の本として検索 | `ValueError` |
| 2 | `ai.search(q, {"題名": "説明文"})` | 説明文を捨てて題名だけ検索 | `ValueError` |
| 3 | **本体のベクトルの検算** | **1本目しか見ていなかった** | **全件を検査** |
| 4 | `ai.embed(123)` | `'int' object is not iterable` | 読める文言 |
| 5 | 本体が壊れた値を返す | `could not convert string to float: '文'` | `RuntimeError` |
| 6 | 件数の上限なし | 100万件で 3907 往復してブラウザが落ちる | 10,000 件で停止 |

### 3番だけ、次の同梱で取り込んでいただくのがよいと思います

**これはこちらの実装の穴でした。** 前回お伝えした「次元の検算」は1本目だけを見ており、
途中から長さの違うベクトルが混じると素通りします。そのうえ内積が `zip` で短いほうに
切りそろえるため、**エラーにならず、近い順だけが静かに狂います**。

全ベクトルを検査し、内積も長さが違えば拒むようにしました。ほかの5件は書き間違いを
止めるだけなので、正しく書かれた教材の動きは変わりません。

もう1件、こちらの実装ミスがありました。埋め込みモデルの二重読み込みを防ぐ鍵に
**生成用の鍵を使い回して**おり、生成中に `embed()` を呼ぶとノートブックごと固まる状態
でした。鍵を分けてあります。

## 4. 本体側にお願いすること

- **`dim: 384` を返し続けていただければ、何も起きません。** `ai-embed` の契約は不変です
- 次の同梱を 0.6.1 に上げていただけると、上の3番が塞がります
- `ai-stream` はいつでも構いません

## 5. 検証したこと

- **446 テスト**が通ります
- **変異テスト8件** — 上の修正を1つずつ壊して、対応するテストが落ちることを確認
- **PyPI に出た 0.6.1 の wheel を隔離環境に入れ、上の6件すべてを実際に叩いて確認**しました

## 6. こちらで確認できていないこと（実機のお願い）

| 項目 | 確かめ方 |
| --- | --- |
| PyHiroba で `ai.search()` が期待どおりか | `book_search.ipynb` を通しで実行 |
| `talk.form()` が PyHiroba で動くか | `chat.ipynb` を通しで実行 |
| `ai.recommend()` が端末を正しく見るか | セルで `await ai.recommend()` |

引き続きよろしくお願いいたします。
